@using Microsoft.AspNetCore.Http;
@using Ohmium.Utilities;
@using Ohmium.Models.EFModels;
@{

    ////  var routeValues = ViewContext.HttpContext.Request.RouteValues.ToList();
    //string _layout = string.Empty;

    //           User info = ViewContext.HttpContext.Session.Get<User>("User");
    //           string user = info == null ? null : info.Login;

    ////  if (routeValues != null)
    //{
    //    //  string cntrolName = routeValues[0].Value.ToString();
    //    // if (cntrolName == "Login" || TempData["User"] == null)
    //    if (user == null)
    //    {

    //        _layout = "~/Views/Shared/_LoginLayout.cshtml";
    //        //return Redirect("~/Views/Home/Index.cshtml");
    //    }
    //    else
    //    {
    //        _layout = "~/Views/Shared/_Layout.cshtml";
    //    }
    //}
    Layout = "~/Views/Shared/_Layout.cshtml";

}
